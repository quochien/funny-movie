<%= form_for(:user, url: session_path(:user)) do |f| %>
  <%= f.text_field :email, placeholder: 'email' %>
  <%= f.password_field :password, placeholder: 'password' %>
  <%= f.submit 'Login' %>
  <%= link_to 'Register', new_password_path(:user) %>
<% end %>
